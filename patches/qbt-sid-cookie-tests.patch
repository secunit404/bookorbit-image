diff --git a/server/src/modules/book-request/download-clients/adapters/qbittorrent.adapter.test.ts b/server/src/modules/book-request/download-clients/adapters/qbittorrent.adapter.test.ts
index c879ea81..3f4b27ea 100644
--- a/server/src/modules/book-request/download-clients/adapters/qbittorrent.adapter.test.ts
+++ b/server/src/modules/book-request/download-clients/adapters/qbittorrent.adapter.test.ts
@@ -25,8 +25,9 @@ function config(overrides: Partial<ResolvedClientConfig> = {}): ResolvedClientCo
 function response(body: string | object, init: { status?: number; setCookie?: string } = {}): Response {
   const headers = new Headers();
   if (init.setCookie) headers.append('set-cookie', init.setCookie);
+  const status = init.status ?? 200;
   const payload = typeof body === 'string' ? body : JSON.stringify(body);
-  return new Response(payload, { status: init.status ?? 200, headers });
+  return new Response(status === 204 ? null : payload, { status, headers });
 }
 
 function mockFetch() {
@@ -538,6 +539,31 @@ describe('QbittorrentAdapter', () => {
       expect((infoCall?.init.headers as Record<string, string>).Cookie).toBe('SID=abc123');
     });
 
+    /** qBittorrent 5.2 renamed the session cookie to QBT_SID_<port> and answers the login with 204. */
+    it('sends back the port-specific session cookie from a 204 login', async () => {
+      const { calls, handlers } = mockFetch();
+      handlers.set('/auth/login', () => response('', { status: 204, setCookie: 'QBT_SID_8080=abc123; HttpOnly; path=/' }));
+      handlers.set('/torrents/info', () => response([]));
+
+      await adapter.status([INFO_HASH], config());
+
+      expect(calls.filter((call) => call.url.includes('/auth/login'))).toHaveLength(1);
+      const infoCall = calls.find((call) => call.url.includes('/torrents/info'));
+      expect((infoCall?.init.headers as Record<string, string>).Cookie).toBe('QBT_SID_8080=abc123');
+    });
+
+    /** The suffix is qBittorrent's own WebUI port, which differs from the URL port behind a proxy. */
+    it('keeps the cookie name qBittorrent chose when its port differs from the configured one', async () => {
+      const { calls, handlers } = mockFetch();
+      handlers.set('/auth/login', () => response('', { status: 204, setCookie: 'QBT_SID_18022=xyz789; HttpOnly; path=/' }));
+      handlers.set('/torrents/info', () => response([]));
+
+      await adapter.status([INFO_HASH], config({ baseUrl: 'http://127.0.0.1:9000' }));
+
+      const infoCall = calls.find((call) => call.url.includes('/torrents/info'));
+      expect((infoCall?.init.headers as Record<string, string>).Cookie).toBe('QBT_SID_18022=xyz789');
+    });
+
     /** An expired SID answers 403 on every endpoint and looks exactly like a permission problem. */
     it('re-authenticates once on a 403 and then gives up', async () => {
       const { calls, handlers } = mockFetch();
